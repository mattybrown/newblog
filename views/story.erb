<%= erb :left_column %>
<div class="col-md-9">
  <img src="<%= @story.file %>" class="img-rounded img-responsive" />
  <p><small><%= pretty_date(@story.created_at) %></small></p>
  <h2><%= @story.title %></h2>
  <p><%= @story.body %></p>
</div>
